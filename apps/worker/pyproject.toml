[project]
name = "rag-worker"
version = "0.1.0"
description = "Dramatiq background worker for RAG Agent"
requires-python = ">=3.12,<3.13"
dependencies = [
    "dramatiq[redis]>=2.2,<3",
    "rag-core",
]

[build-system]
requires = ["uv-build>=0.10.3,<0.13"]
build-backend = "uv_build"
